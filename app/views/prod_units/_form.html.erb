<%= form_with(model: prod_unit) do |form| %>
  <% if prod_unit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prod_unit.errors.count, "error") %> prohibited this prod_unit from being saved:</h2>

      <ul>
        <% prod_unit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <br />

  <div>
    <%= form.submit %>
  </div>
<% end %>
