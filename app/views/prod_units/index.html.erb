<div class="container"><p style="color: green"><%= notice %></p>

<h1>Prod units</h1>

<div id="prod_units">

  <table class="table table-striped table-bordered table-condensed">
    <tbody>
      
      <% @prod_units.each do |prod_unit| %>
        <tr>
          <td><%= render prod_unit %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= link_to "New prod unit", new_prod_unit_path %>
</div>