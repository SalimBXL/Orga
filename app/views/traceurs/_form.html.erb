<%= form_with(model: traceur) do |form| %>
  <% if traceur.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(traceur.errors.count, "error") %> prohibited this traceur from being saved:</h2>

      <ul>
        <% traceur.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
