<%= form_with(model: maintenance_ressource) do |form| %>
  <% if maintenance_ressource.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(maintenance_ressource.errors.count, "error") %> prohibited this maintenance_ressource from being saved:</h2>

      <ul>
        <% maintenance_ressource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :service_id, style: "display: block" %>
    <%= form.select(:service_id) do %>
                        <%= select_option_tag("", nil, @maintenance_ressource.service_id) %>
                        <% @services.each do |l| %>
                            <%= select_option_tag(l.nom, l.id, @maintenance_ressource.service_id) %>
                        <% end %>
                    <% end %>
  </div>

  <p>&nbsp;</p>

  <div>
    <%= form.submit %>
  </div>
<% end %>
